# Gradle 配置
org.gradle.jvmargs=-Xmx4096m -XX:MaxMetaspaceSize=1024m -XX:+HeapDumpOnOutOfMemoryError -XX:+UseG1GC -Dfile.encoding=UTF-8
org.gradle.caching=true

# 守护进程优化配置
org.gradle.daemon=true
# 空闲 10 分钟后自动关闭（避免长时间锁定文件）
org.gradle.daemon.idletimeout=600000
# 最大守护进程数量
org.gradle.daemon.count=2

# 性能优化
org.gradle.parallel=true
org.gradle.configureondemand=true
# 配置缓存（加速构建，Gradle 8.x+ 推荐）
org.gradle.configuration-cache=true
org.gradle.configuration-cache.problems=warn

# 文件监控（自动检测文件变化）
org.gradle.vfs.watch=true

# Kotlin 配置
kotlin.code.style=official

# ------------------------------------------------------------------
# IntelliJ Platform Plugin Properties
#
# The following properties are used by the IntelliJ Platform Gradle Plugin
# to configure the project. Read more: https://plugins.jetbrains.com/docs/intellij/tools-intellij-platform-gradle-plugin-properties.html
# ------------------------------------------------------------------

# IntelliJ Platform Type and Version
platformType = IU
platformVersion = 2025.3.1

# Plugin Version
pluginVersion = 1.0.9

# Plugin Signing
# signPlugin.keystore =
# signPlugin.keystore.password =
# signPlugin.keystore.key.alias =
# signPlugin.keystore.key.password =

# Publish Plugin
# publishPlugin.token =

# Java Configuration
javaVersion = 21

# Plugin Dependencies
# pluginDependencies =

# Plugin Metadata
pluginGroup = com.asakii
pluginName = Claude Code Plus
pluginSinceBuild = 242
pluginUntilBuild = 253.*

# OSSRH 发布凭据（已配置在 %USERPROFILE%\.gradle\gradle.properties 中）
# ossrhUsername=
# ossrhPassword=
# 如果需要，可在本机 Gradle 用户配置中覆写以下 GPG 信息
signing.gnupg.executable=C:/Program Files/Git/usr/bin/gpg.exe
# signing.gnupg.keyName=
# signing.gnupg.passphrase=