# Claude Code Plus 国际化系统

## 概述

Claude Code Plus 支持多语言国际化，能够根据 IDE 的语言设置自动切换界面语言。

## 支持的语言

- 🇺🇸 **English** (英语) - 默认语言
- 🇨🇳 **简体中文** (Simplified Chinese)  
- 🇭🇰 **繁體中文** (Traditional Chinese)
- 🇯🇵 **日本語** (Japanese)
- 🇰🇷 **한국어** (Korean)

## 技术架构

### 核心组件

1. **LocalizationService** - 语言检测和映射服务
2. **StringResources** - 字符串资源管理器
3. **IDELocalizationService** - IDE 语言设置获取服务

### 文件结构

```
toolwindow/src/main/
├── composeResources/
│   ├── values/strings.xml           # 默认(英语)
│   ├── values-zh-CN/strings.xml     # 简体中文
│   ├── values-zh-TW/strings.xml     # 繁体中文
│   ├── values-ja/strings.xml        # 日语
│   └── values-ko/strings.xml        # 韩语
└── kotlin/com/claudecodeplus/ui/services/
    ├── LocalizationService.kt       # 语言映射服务
    └── StringResources.kt           # 字符串资源管理
```

## 使用方法

### 1. 获取本地化字符串

```kotlin
import com.claudecodeplus.ui.services.stringResource

@Composable
fun MyComponent() {
    Text(stringResource("chat_input_placeholder"))
}
```

### 2. 带参数的字符串格式化

```kotlin
import com.claudecodeplus.ui.services.formatStringResource

@Composable
fun FileInfo(size: String) {
    Text(formatStringResource("file_size", size))
}
```

### 3. 获取当前语言信息

```kotlin
val currentLanguage = LocalizationService.getCurrentLanguage()
val isCJK = LocalizationService.isCJKLanguage()
```

## 字符串资源示例

### 核心UI文本

| 键名 | 英语 | 简体中文 | 繁体中文 | 日语 | 韩语 |
|------|------|---------|---------|------|------|
| `send` | Send | 发送 | 傳送 | 送信 | 전송 |
| `cancel` | Cancel | 取消 | 取消 | キャンセル | 취소 |
| `chat_input_placeholder` | Type a message... | 输入消息... | 輸入訊息... | メッセージを入力... | 메시지를 입력하세요... |

### 工具名称

| 键名 | 英语 | 简体中文 | 繁体中文 | 日语 | 韩语 |
|------|------|---------|---------|------|------|
| `tool_read` | Read | 读取 | 讀取 | 読み取り | 읽기 |
| `tool_edit` | Edit | 编辑 | 編輯 | 編集 | 편집 |
| `tool_executing` | Executing... | 执行中... | 執行中... | 实行中... | 실행 중... |

## 自动语言检测

系统会自动检测以下IDE语言设置：

- `zh-CN` → 简体中文
- `zh-TW`, `zh-HK` → 繁体中文  
- `ja` → 日语
- `ko` → 韩语
- 其他 → 英语 (默认)

## 添加新语言

### 1. 创建资源文件

```bash
mkdir toolwindow/src/main/composeResources/values-[LANGUAGE_CODE]
```

### 2. 添加字符串翻译

创建 `strings.xml` 文件并添加所有字符串的翻译。

### 3. 更新语言枚举

在 `LocalizationService.kt` 中添加新的 `SupportedLanguage` 枚举值。

### 4. 更新映射逻辑

在 `mapToSupportedLanguage()` 函数中添加语言检测逻辑。

## 开发指南

### 最佳实践

1. **统一使用资源系统**
   ```kotlin
   // ✅ 正确
   Text(stringResource("button_text"))
   
   // ❌ 错误
   Text("Button Text")
   ```

2. **参数化字符串**
   ```kotlin
   // ✅ 正确
   formatStringResource("file_size", "1.2MB")
   
   // ❌ 错误  
   stringResource("file_size") + "1.2MB"
   ```

3. **处理CJK语言的特殊需求**
   ```kotlin
   val isCJK = LocalizationService.isCJKLanguage()
   val fontSize = if (isCJK) 14.sp else 13.sp
   ```

### 测试

运行国际化测试：
```bash
./gradlew :toolwindow:test --tests "*LocalizationServiceTest"
```

## 当前状态

✅ **已完成**
- 核心国际化架构
- 5种语言支持 (英/简中/繁中/日/韩)
- 自动语言检测
- 字符串资源管理系统
- 完整测试套件

🚧 **待完成**
- 更多UI组件的字符串替换
- 日期时间本地化
- 数字格式本地化
- 动态语言切换

## 注意事项

1. **插件环境限制**: 在插件环境中，Compose资源系统可能不工作，因此使用了手动管理的字符串映射方式。

2. **IDE语言获取**: `toolwindow` 模块暂时使用系统默认语言，真正的IDE语言设置需要通过 `jetbrains-plugin` 模块的 `IDELocalizationService` 获取。

3. **字体支持**: CJK语言可能需要特殊的字体配置以确保正确显示。