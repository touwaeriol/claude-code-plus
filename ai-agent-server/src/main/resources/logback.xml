<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <!--
        禁用控制台输出，只使用文件日志。
        原因：当服务器作为子进程运行时，stdout/stderr 会被父进程捕获并再次记录为日志，形成死循环。
        文件日志由 StandaloneLogging 动态配置到 <project>/.log/ 目录。
    -->

    <!-- 默认不输出任何日志到控制台，StandaloneLogging 会配置文件 appender -->
    <root level="INFO">
        <!-- 无 appender，日志由 StandaloneLogging 动态添加 -->
    </root>
</configuration>

